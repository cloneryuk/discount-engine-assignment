{
  "name": "opptra-discount-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve src -l 5173",
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
